http://www.dotnetperls.com/shuffle-java

public void shuffle(int[] arr) {
    int n = arr.length;
    for(int i=0; i < n; i++) {
        int random = i + (int) (Math.random() *(n-i));
        int temp = arr[random];
        arr[random] = arr[i];
        arr[i] = temp;
    }
}
